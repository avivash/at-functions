{
  "name": "at-functions",
  "version": "0.1.0",
  "description": "POC: AT Protocol as a code registry with Fastify WASM execution runtime",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "dev": "bun --watch src/server.ts",
    "start": "bun dist/server.js",
    "test": "bun test"
  },
  "dependencies": {
    "@atproto/api": "^0.13.33",
    "@bytecodealliance/jco": "^1.6.0",
    "fastify": "^5.3.2"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "bun-types": "^1.2.0",
    "typescript": "^5.8.3"
  }
}
